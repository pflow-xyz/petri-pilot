startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    () => ({
      command: "petri-pilot",
      args: ["mcp"]
    })

build:
  dockerfile: Dockerfile
  dockerBuildPath: .
