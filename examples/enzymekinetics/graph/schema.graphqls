# Code generated by petri-pilot. DO NOT EDIT.
# GraphQL schema for enzyme-kinetics

scalar Time

type Query {
  # Get aggregate state by ID
  enzymekinetics(id: ID!): AggregateState

  # List aggregates with optional filtering
  enzymekineticsList(place: String, page: Int, perPage: Int): AggregateList!

  # Event history for an aggregate
  events(aggregateId: ID!, from: Int): [Event!]!

  # Get state at specific version
  stateAtVersion(aggregateId: ID!, version: Int!): AggregateState
}

type Mutation {
  # Create a new enzyme-kinetics instance
  createEnzymekinetics: AggregateState!

  # Substrate binds to enzyme forming complex (rate k1)
  bind(input: BindInput!): TransitionResult!

  # Complex dissociates back to substrate and enzyme (rate k-1)
  unbind(input: UnbindInput!): TransitionResult!

  # Complex converts substrate to product, releasing enzyme (rate kcat)
  catalyze(input: CatalyzeInput!): TransitionResult!
}

# Aggregate state representation
type AggregateState {
  id: ID!
  version: Int!
  state: State!
  places: Places!
  enabledTransitions: [String!]!
}

# Workflow state with all places
type State {
  substrate: Int!
  enzyme: Int!
  complex: Int!
  product: Int!
}

# Token counts for each place
type Places {
  substrate: Int!
  enzyme: Int!
  complex: Int!
  product: Int!
}

# Result of a transition execution
type TransitionResult {
  success: Boolean!
  aggregateId: ID
  version: Int
  state: Places
  enabledTransitions: [String!]
  error: String
}

# Paginated list of aggregates
type AggregateList {
  items: [AggregateState!]!
  total: Int!
  page: Int!
  perPage: Int!
}


# Event record
type Event {
  id: ID!
  streamId: String!
  type: String!
  version: Int!
  timestamp: Time!
  data: String!
}


# Input types for mutations

input BindInput {
  aggregateId: ID!
}


input UnbindInput {
  aggregateId: ID!
}


input CatalyzeInput {
  aggregateId: ID!
}

