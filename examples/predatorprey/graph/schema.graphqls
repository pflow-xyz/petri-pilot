# Code generated by petri-pilot. DO NOT EDIT.
# GraphQL schema for predator-prey

scalar Time

type Query {
  # Get aggregate state by ID
  predatorprey(id: ID!): AggregateState

  # List aggregates with optional filtering
  predatorpreyList(place: String, page: Int, perPage: Int): AggregateList!

  # Event history for an aggregate
  events(aggregateId: ID!, from: Int): [Event!]!

  # Get state at specific version
  stateAtVersion(aggregateId: ID!, version: Int!): AggregateState
}

type Mutation {
  # Create a new predator-prey instance
  createPredatorprey: AggregateState!

  # Prey natural reproduction (birth rate)
  preyReproduce(input: PreyReproduceInput!): TransitionResult!

  # Predator eats prey (predation rate)
  predation(input: PredationInput!): TransitionResult!

  # Predator natural death (death rate)
  predatorDeath(input: PredatorDeathInput!): TransitionResult!
}

# Aggregate state representation
type AggregateState {
  id: ID!
  version: Int!
  state: State!
  places: Places!
  enabledTransitions: [String!]!
}

# Workflow state with all places
type State {
  prey: Int!
  predator: Int!
}

# Token counts for each place
type Places {
  prey: Int!
  predator: Int!
}

# Result of a transition execution
type TransitionResult {
  success: Boolean!
  aggregateId: ID
  version: Int
  state: Places
  enabledTransitions: [String!]
  error: String
}

# Paginated list of aggregates
type AggregateList {
  items: [AggregateState!]!
  total: Int!
  page: Int!
  perPage: Int!
}


# Event record
type Event {
  id: ID!
  streamId: String!
  type: String!
  version: Int!
  timestamp: Time!
  data: String!
}


# Input types for mutations

input PreyReproduceInput {
  aggregateId: ID!
}


input PredationInput {
  aggregateId: ID!
}


input PredatorDeathInput {
  aggregateId: ID!
}

