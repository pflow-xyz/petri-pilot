# Code generated by petri-pilot. DO NOT EDIT.
# GraphQL schema for stoplight

scalar Time

type Query {
  # Get aggregate state by ID
  stoplight(id: ID!): AggregateState

  # List aggregates with optional filtering
  stoplightList(place: String, page: Int, perPage: Int): AggregateList!

  # Event history for an aggregate
  events(aggregateId: ID!, from: Int): [Event!]!

  # Get state at specific version
  stateAtVersion(aggregateId: ID!, version: Int!): AggregateState
}

type Mutation {
  # Create a new stoplight instance
  createStoplight: AggregateState!

  # Red to green
  go(input: GoInput!): TransitionResult!

  # Green to yellow
  slow(input: SlowInput!): TransitionResult!

  # Yellow to red
  stop(input: StopInput!): TransitionResult!
}

# Aggregate state representation
type AggregateState {
  id: ID!
  version: Int!
  state: State!
  places: Places!
  enabledTransitions: [String!]!
}

# Workflow state with all places
type State {
  red: Int!
  green: Int!
  yellow: Int!
}

# Token counts for each place
type Places {
  red: Int!
  green: Int!
  yellow: Int!
}

# Result of a transition execution
type TransitionResult {
  success: Boolean!
  aggregateId: ID
  version: Int
  state: Places
  enabledTransitions: [String!]
  error: String
}

# Paginated list of aggregates
type AggregateList {
  items: [AggregateState!]!
  total: Int!
  page: Int!
  perPage: Int!
}


# Event record
type Event {
  id: ID!
  streamId: String!
  type: String!
  version: Int!
  timestamp: Time!
  data: String!
}


# Input types for mutations

input GoInput {
  aggregateId: ID!
}


input SlowInput {
  aggregateId: ID!
}


input StopInput {
  aggregateId: ID!
}

