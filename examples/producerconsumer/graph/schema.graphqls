# Code generated by petri-pilot. DO NOT EDIT.
# GraphQL schema for producer-consumer

scalar Time

type Query {
  # Get aggregate state by ID
  producerconsumer(id: ID!): AggregateState

  # List aggregates with optional filtering
  producerconsumerList(place: String, page: Int, perPage: Int): AggregateList!

  # Event history for an aggregate
  events(aggregateId: ID!, from: Int): [Event!]!

  # Get state at specific version
  stateAtVersion(aggregateId: ID!, version: Int!): AggregateState
}

type Mutation {
  # Create a new producer-consumer instance
  createProducerconsumer: AggregateState!

  # Producer begins creating an item (requires buffer space)
  startProduce(input: StartProduceInput!): TransitionResult!

  # Producer finishes item, places it in buffer
  finishProduce(input: FinishProduceInput!): TransitionResult!

  # Consumer takes an item from the buffer
  startConsume(input: StartConsumeInput!): TransitionResult!

  # Consumer finishes processing the item
  finishConsume(input: FinishConsumeInput!): TransitionResult!
}

# Aggregate state representation
type AggregateState {
  id: ID!
  version: Int!
  state: State!
  places: Places!
  enabledTransitions: [String!]!
}

# Workflow state with all places
type State {
  producerIdle: Int!
  producing: Int!
  buffer: Int!
  bufferSpace: Int!
  consumerIdle: Int!
  consuming: Int!
  consumed: Int!
}

# Token counts for each place
type Places {
  producerIdle: Int!
  producing: Int!
  buffer: Int!
  bufferSpace: Int!
  consumerIdle: Int!
  consuming: Int!
  consumed: Int!
}

# Result of a transition execution
type TransitionResult {
  success: Boolean!
  aggregateId: ID
  version: Int
  state: Places
  enabledTransitions: [String!]
  error: String
}

# Paginated list of aggregates
type AggregateList {
  items: [AggregateState!]!
  total: Int!
  page: Int!
  perPage: Int!
}


# Event record
type Event {
  id: ID!
  streamId: String!
  type: String!
  version: Int!
  timestamp: Time!
  data: String!
}


# Input types for mutations

input StartProduceInput {
  aggregateId: ID!
}


input FinishProduceInput {
  aggregateId: ID!
}


input StartConsumeInput {
  aggregateId: ID!
}


input FinishConsumeInput {
  aggregateId: ID!
}

