# Code generated by petri-pilot. DO NOT EDIT.
# GraphQL schema for loan-approval

scalar Time

type Query {
  # Get aggregate state by ID
  loanapproval(id: ID!): AggregateState

  # List aggregates with optional filtering
  loanapprovalList(place: String, page: Int, perPage: Int): AggregateList!

  # Event history for an aggregate
  events(aggregateId: ID!, from: Int): [Event!]!

  # Get state at specific version
  stateAtVersion(aggregateId: ID!, version: Int!): AggregateState
}

type Mutation {
  # Create a new loan-approval instance
  createLoanapproval: AggregateState!

  # Begin parallel credit and employment reviews
  startReviews(input: StartReviewsInput!): TransitionResult!

  # Approve credit check
  approveCredit(input: ApproveCreditInput!): TransitionResult!

  # Reject credit check
  rejectCredit(input: RejectCreditInput!): TransitionResult!

  # Approve employment verification
  approveEmployment(input: ApproveEmploymentInput!): TransitionResult!

  # Reject employment verification
  rejectEmployment(input: RejectEmploymentInput!): TransitionResult!

  # Synchronize: both reviews must pass
  mergeReviews(input: MergeReviewsInput!): TransitionResult!

  # Begin final underwriter review
  beginUnderwriting(input: BeginUnderwritingInput!): TransitionResult!

  # Approve the loan application
  approveLoan(input: ApproveLoanInput!): TransitionResult!

  # Reject the loan application
  rejectLoan(input: RejectLoanInput!): TransitionResult!

  # Reject due to failed credit check
  rejectOnCredit(input: RejectOnCreditInput!): TransitionResult!

  # Reject due to failed employment verification
  rejectOnEmployment(input: RejectOnEmploymentInput!): TransitionResult!
}

# Aggregate state representation
type AggregateState {
  id: ID!
  version: Int!
  state: State!
  places: Places!
  enabledTransitions: [String!]!
}

# Workflow state with all places
type State {
  submitted: Int!
  creditReview: Int!
  employmentReview: Int!
  creditPassed: Int!
  creditFailed: Int!
  employmentPassed: Int!
  employmentFailed: Int!
  readyForDecision: Int!
  underReview: Int!
  approved: Int!
  rejected: Int!
}

# Token counts for each place
type Places {
  submitted: Int!
  creditReview: Int!
  employmentReview: Int!
  creditPassed: Int!
  creditFailed: Int!
  employmentPassed: Int!
  employmentFailed: Int!
  readyForDecision: Int!
  underReview: Int!
  approved: Int!
  rejected: Int!
}

# Result of a transition execution
type TransitionResult {
  success: Boolean!
  aggregateId: ID
  version: Int
  state: Places
  enabledTransitions: [String!]
  error: String
}

# Paginated list of aggregates
type AggregateList {
  items: [AggregateState!]!
  total: Int!
  page: Int!
  perPage: Int!
}


# Event record
type Event {
  id: ID!
  streamId: String!
  type: String!
  version: Int!
  timestamp: Time!
  data: String!
}


# Input types for mutations

input StartReviewsInput {
  aggregateId: ID!
}


input ApproveCreditInput {
  aggregateId: ID!
}


input RejectCreditInput {
  aggregateId: ID!
}


input ApproveEmploymentInput {
  aggregateId: ID!
}


input RejectEmploymentInput {
  aggregateId: ID!
}


input MergeReviewsInput {
  aggregateId: ID!
}


input BeginUnderwritingInput {
  aggregateId: ID!
}


input ApproveLoanInput {
  aggregateId: ID!
}


input RejectLoanInput {
  aggregateId: ID!
}


input RejectOnCreditInput {
  aggregateId: ID!
}


input RejectOnEmploymentInput {
  aggregateId: ID!
}

