// Code generated by petri-pilot. DO NOT EDIT.

package main

import (
	"fmt"
)

// Role constants
const (
	RoleApplicant = "applicant"
	RoleSystem = "system"
	RoleUnderwriter = "underwriter"
	RoleAdmin = "admin"
)

// AllRolesFor maps each role to its effective roles (including inherited).
// For example, if admin inherits from user, AllRolesFor[RoleAdmin] = []string{"admin", "user"}.
var AllRolesFor = map[string][]string{
	RoleApplicant: { "applicant" },
	RoleSystem: { "system" },
	RoleUnderwriter: { "underwriter" },
	RoleAdmin: { "admin", "underwriter" },
}

// HasRole checks if a user has a specific role (including inherited roles).
func HasRole(user *User, roleID string) bool {
	if user == nil {
		return false
	}

	// Get effective roles for each of the user's roles
	for _, userRole := range user.Roles {
		effectiveRoles, ok := AllRolesFor[userRole]
		if !ok {
			// Role not in hierarchy, just check direct match
			if userRole == roleID {
				return true
			}
			continue
		}

		for _, effective := range effectiveRoles {
			if effective == roleID {
				return true
			}
		}
	}

	return false
}

// HasAnyRole checks if a user has at least one of the specified roles.
func HasAnyRole(user *User, roleIDs []string) bool {
	if user == nil || len(roleIDs) == 0 {
		return false
	}

	for _, roleID := range roleIDs {
		if HasRole(user, roleID) {
			return true
		}
	}

	return false
}

// ErrUnauthorized is returned when a user is not authenticated.
var ErrUnauthorized = fmt.Errorf("unauthorized: authentication required")

// ErrForbidden is returned when a user lacks permission for an action.
var ErrForbidden = fmt.Errorf("forbidden: insufficient permissions")

// CheckRoleAccess verifies that a user has one of the required roles.
// Returns nil if access is granted, ErrUnauthorized if user is nil,
// or ErrForbidden if user lacks required roles.
func CheckRoleAccess(user *User, requiredRoles []string) error {
	if user == nil {
		return ErrUnauthorized
	}

	if len(requiredRoles) == 0 {
		// No role requirement - any authenticated user allowed
		return nil
	}

	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}

	return nil
}


// CheckAccessRunCreditCheck verifies access for the run_credit_check transition.
func CheckAccessRunCreditCheck(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "system" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessAutoApprove verifies access for the auto_approve transition.
func CheckAccessAutoApprove(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "system" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessFlagForReview verifies access for the flag_for_review transition.
func CheckAccessFlagForReview(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "system" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessAutoDeny verifies access for the auto_deny transition.
func CheckAccessAutoDeny(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "system" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessUnderwriterApprove verifies access for the underwriter_approve transition.
func CheckAccessUnderwriterApprove(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "underwriter" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessUnderwriterDeny verifies access for the underwriter_deny transition.
func CheckAccessUnderwriterDeny(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "underwriter" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessFinalizeApproval verifies access for the finalize_approval transition.
func CheckAccessFinalizeApproval(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "system" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessDisburse verifies access for the disburse transition.
func CheckAccessDisburse(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "admin" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessStartRepayment verifies access for the start_repayment transition.
func CheckAccessStartRepayment(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "system" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessMakePayment verifies access for the make_payment transition.
func CheckAccessMakePayment(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "applicant" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessComplete verifies access for the complete transition.
func CheckAccessComplete(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "system" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}

// CheckAccessMarkDefault verifies access for the mark_default transition.
func CheckAccessMarkDefault(user *User, state map[string]any) error {
	if user == nil {
		return ErrUnauthorized
	}
	
	// Check role requirements
	requiredRoles := []string{ "admin" }
	if !HasAnyRole(user, requiredRoles) {
		return ErrForbidden
	}
	
	
	return nil
}


// GetUserRoles returns the effective roles for a user (with inheritance expanded).
func GetUserRoles(user *User) []string {
	if user == nil {
		return nil
	}

	seen := make(map[string]bool)
	var result []string

	for _, role := range user.Roles {
		effectiveRoles, ok := AllRolesFor[role]
		if !ok {
			// Role not in hierarchy, add directly
			if !seen[role] {
				seen[role] = true
				result = append(result, role)
			}
			continue
		}

		for _, r := range effectiveRoles {
			if !seen[r] {
				seen[r] = true
				result = append(result, r)
			}
		}
	}

	return result
}

